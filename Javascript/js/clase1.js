alert("esto fue escrito en un archivo.js"); // función alert con cadena de texto.
alert(150); // Alerta con números, sin comillas dobles.
alert(false); // alerta con valor falso de tipo voleano.
var vaso ="agua"; // Creamos variable global de tipo texto.
let cantidad = 5; // Creamos variable local de tipo número
const sobrante = 3; // Creamos variable de constante de tipo número.
alert(vaso+cantidad); // mostramos una alerta sumando la variable vaso más la variable cantidad.
alert(cantidad+sobrante); // Mostramos una alerta con el resultado de cantidad más sobrante.
cantidad = 9; // Modificamos el valor de cantidad.
alert(cantidad-sobrante); // A cantidad le restamos sobrante.
alert("Tenemos "+cantidad+"porciones de "+vaso); // creamos una alerta concatenando cadenas de texto con variables.